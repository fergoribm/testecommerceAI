<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <title>Checkout - Cloud Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h2>Il tuo Carrello</h2>
        <div class="card p-4 shadow">
            <% if (cart.length > 0) { %>
                <ul class="list-group mb-3">
                    <% cart.forEach(item => { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <%= item.name %>
                            <span>€ <%= item.price %></span>
                        </li>
                    <% }) %>
                </ul>
                <h4 class="text-end">Totale: € <%= total %></h4>
                <button class="btn btn-primary btn-lg w-100 mt-3" onclick="alert('Ordine simulato con successo!')">Conferma Ordine</button>
            <% } else { %>
                <p>Il carrello è vuoto.</p>
            <% } %>
            <a href="/" class="btn btn-link mt-2">Torna allo shopping</a>
        </div>
    </div>
</body>
</html>
