<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IT Tech Shop</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .product-img { height: 200px; object-fit: cover; }
        .card { transition: transform 0.2s; }
        .card:hover { transform: scale(1.03); }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">💻 IT-STORE</a>
            <div class="d-flex">
                <a href="/checkout" class="btn btn-light position-relative">
                    🛒 Carrello
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                        <%= cartCount %>
                    </span>
                </a>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <header class="text-center mb-5">
            <h1 class="display-4 fw-bold">Catalogo Hardware 2024</h1>
            <p class="lead">Componenti e periferiche professionali pronte per il deploy.</p>
        </header>

        <div class="row">
            <% products.forEach(product => { %>
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="card border-0 shadow-sm h-100">
                        <img src="<%= product.img %>" class="card-img-top product-img" alt="<%= product.name %>">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title fw-bold"><%= product.name %></h5>
                            <p class="card-text text-muted small"><%= product.desc %></p>
                            <div class="mt-auto">
                                <p class="h4 text-primary mb-3">€ <%= product.price.toFixed(2) %></p>
                                <form action="/add-to-cart" method="POST">
                                    <input type="hidden" name="productId" value="<%= product.id %>">
                                    <button type="submit" class="btn btn-outline-primary w-100">Aggiungi</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
