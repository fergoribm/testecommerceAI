<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node Shop - CodeEngine</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">Cloud Shop</a>
            <a href="/checkout" class="btn btn-outline-light">
                Carrello (<%= cartCount %>)
            </a>
        </div>
    </nav>

    <div class="container">
        <h1 class="mb-4">Prodotti in primo piano</h1>
        <div class="row">
            <% products.forEach(product => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= product.img %>" class="card-img-top" alt="<%= product.name %>">
                        <div class="card-body text-center">
                            <h5 class="card-title"><%= product.name %></h5>
                            <p class="card-text text-primary fw-bold">€ <%= product.price %></p>
                            <form action="/add-to-cart" method="POST">
                                <input type="hidden" name="productId" value="<%= product.id %>">
                                <button type="submit" class="btn btn-success w-100">Aggiungi al carrello</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
